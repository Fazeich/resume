import { Quote, Section, SectionTitle } from "../lib/styles";

export const MissionStatement = () => {
  return (
    <Section>
      <SectionTitle>🧭 Mission Statement</SectionTitle>
      <Quote>
        “I transform complex business logic into seamless user experiences. My
        goal is to build elegant, scalable, and lightning-fast interfaces that
        users love and developers admire.”
      </Quote>
    </Section>
  );
};
