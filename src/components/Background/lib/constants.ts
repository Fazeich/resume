export const TRIANGLE_CONFIG = {
  width: 2.5,
  size: Math.random() * 30 + 13,
  speed: {
    x: (Math.random() - 0.25) * 2,
    y: (Math.random() - 0.35) * 2,
  },
  angle: Math.random() * 2 * Math.PI,
  angularSpeed: (Math.random() - 0.5) * 0.02,
};

export const GRADIENT_SIZE = 300;

export const SPEED_ACCELERATION = 0.2;

export const MAX_SPEED = 2;
